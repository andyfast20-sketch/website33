# Copy this file to .env (same folder as vonage_agent.py) and fill in your keys.
# Do NOT commit .env to source control.

## NEVER COMMIT REAL SECRETS
## Prefer using the Super Admin UI (keys are encrypted at rest in the local DB).

# Optional: Fernet master key used to encrypt secrets stored in call_logs.db.
# If not set, the app will auto-generate and store one in Windows Credential Manager (via keyring).
# WEBSITE33_MASTER_KEY=

# OpenAI
OPENAI_API_KEY=

# Speechmatics TTS
SPEECHMATICS_API_KEY=

# DeepSeek (optional)
DEEPSEEK_API_KEY=
DEEPSEEK_API_KEY_FALLBACK=

# Vonage
VONAGE_API_KEY=
VONAGE_API_SECRET=

# ElevenLabs / Cartesia / PlayHT (optional)
ELEVENLABS_API_KEY=
CARTESIA_API_KEY=
PLAYHT_USER_ID=
PLAYHT_API_KEY=

# Public URL (ngrok)
PUBLIC_URL=

# Google TTS: point to your local service account JSON (do not commit the file)
GOOGLE_CREDENTIALS_PATH=google-credentials.json
